require File.expand_path(File.dirname(__FILE__) + '/../spec_helper')

describe ChildrenFolder do
  before(:each) do
    @valid_attributes = {

    }
  end

  xit "should create a new instance given valid attributes" do
    ChildrenFolder.create!(@valid_attributes)
  end
end
